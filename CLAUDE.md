# CLAUDE.md

此文件为 Claude Code (claude.ai/code) 在该代码仓库中工作时提供指导。

## 项目概述

这是 **Aaron 框架**项目 - 一个基于 Cocos Creator 3.8 开发的 TypeScript 2D 游戏框架。框架采用模块化架构设计，清晰分离框架代码和业务逻辑。

## 核心架构

### 框架结构
- **框架代码**: `assets/scripts/aaron/` - 核心框架模块（当前为空，待实现）
- **业务逻辑**: `assets/scripts/game/` - 游戏特定代码（待创建）
- **文档**: `doc/aaron/` - 完整的框架中文文档

### 核心设计原则
- **模块化架构**: 框架层与业务层清晰分离
- **类型安全**: 完整的 TypeScript 支持，启用装饰器
- **性能优先**: 对象池、资源引用计数、异步加载
- **依赖注入**: 使用装饰器实现 DI
- **UI 框架**: 基于栈的 UI 管理，层级分离

### 框架模块（规划中）
- **核心模块**: 应用程序、事件管理器、对象池、日志系统
- **管理器模块**: UI、资源、音频、网络、场景、数据、计时器、本地化管理器
- **基础类**: 组件、视图、面板、模型、单例基类
- **UI 框架**: 层级管理、栈管理、动画、组件库
- **资源管理**: 加载、缓存、引用计数、Bundle 管理
- **工具函数**: 数学、时间、字符串、数组、对象、节点、对象池、验证工具
- **配置模块**: 游戏、UI、网络、资源、平台配置

## 开发命令

### 构建和开发
这是一个 Cocos Creator 项目，主要通过 Cocos Creator IDE 进行开发：

- **打开项目**: 在 Cocos Creator 3.8.x 中打开此目录
- **构建**: 使用 Cocos Creator 的构建面板（`项目 → 构建`）
- **预览**: 使用 Cocos Creator 的预览功能
- **编译**: TypeScript 编译由 Cocos Creator 自动处理

### TypeScript 配置
- **基础配置**: 继承自 `./temp/tsconfig.cocos.json`（由 Cocos Creator 管理）
- **自定义配置**: 在 `tsconfig.json` 中覆盖（当前设置 `"strict": false`）
- **装饰器**: 已启用（`"experimentalDecorators": true`）
- **路径映射**: 为 Cocos Creator 资源系统配置

## 重要约定

### 文件命名
- 使用全小写字母和连字符：`file-name.ts`
- 框架模块：`assets/scripts/aaron/`
- 业务逻辑：`assets/scripts/game/`

### 语言和文档
- 文档主要使用中文
- 代码注释应遵循项目语言偏好
- 框架设计强调"实用至上"

### 架构模式
- **MVC + 依赖注入**: 核心架构模式
- **事件驱动**: 广泛使用事件系统解耦
- **对象池**: 用于性能优化
- **资源引用计数**: 用于内存管理

## 当前状态

这是一个**框架脚手架项目** - 架构和文档已完成，但实现待定：

- ✅ 完整的架构设计和文档
- ✅ 目录结构规划
- ✅ TypeScript 配置
- ⏳ 框架代码实现（尚未开始）
- ⏳ 业务逻辑模块（框架准备就绪后创建）

## 在此项目中工作

在此项目工作时：

1. **首先阅读文档**: `doc/aaron/index.md` 和 `doc/aaron/architecture/design.md`
2. **遵循规划的结构**: 根据详细的架构计划进行实现
3. **使用 TypeScript 特性**: 利用装饰器、严格类型检查和现代 TS 特性
4. **保持分离**: 框架代码放在 `aaron/`，业务代码放在 `game/`
5. **考虑性能**: 按设计使用对象池、引用计数

该框架专为面向 Web、Android、iOS 和微信小游戏平台的 2D 游戏设计。